[project]
name = "sddtdd-broker-mcp"
version = "1.0.0"
description = "MCP task broker for Spec-Driven TDD"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.6.0",
]

[project.scripts]
sddtdd-broker-mcp = "server:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
